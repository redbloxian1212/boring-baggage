--[=[
    @desc: Server's event paths for EventBus
    @usage:

    {
         PlayerDied = "PlayerDied",
        Sword = {
            OnPriceUpdated = "Swords.OnPriceUpdated",
        }
        Shield = {
            OnPriceUpdated = "Shields.OnPriceUpdated",
        }
    }
]=]

return table.freeze({})
